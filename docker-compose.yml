services:
  tenantly-redis:
    image: redis:7-alpine
    container_name: tenantly-redis
    restart: always
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    networks:
      - postgres_default

  backend:
    build: ./backend
    image: ${DOCKERHUB_USERNAME}/tenantly-api:latest
    pull_policy: always
    container_name: tenantly-api
    restart: always
    environment:
      DATABASE_URL: ${DATABASE_URL}
      JWT_SECRET: ${JWT_SECRET}
      JWT_EXPIRES_IN: ${JWT_EXPIRES_IN}
      PORT: 3001
      CORS_ORIGINS: ${CORS_ORIGINS}
      REDIS_HOST: tenantly-redis
      REDIS_PORT: 6379
      NODE_ENV: production
    depends_on:
      - tenantly-redis
    networks:
      - postgres_default

  frontend:
    build:
      context: ./frontend
      args:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL}
    image: ${DOCKERHUB_USERNAME}/tenantly-web:latest
    pull_policy: always
    container_name: tenantly-web
    restart: always
    networks:
      - postgres_default

  # Single entrypoint for the stack: the only published port, fronted by Caddy.
  gateway:
    image: nginx:alpine
    container_name: tenantly-gateway
    restart: always
    ports:
      - "127.0.0.1:${WEB_PORT}:80"
    volumes:
      - ./nginx-gateway.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - backend
      - frontend
    networks:
      - postgres_default

volumes:
  redis_data:

networks:
  postgres_default:
    external: true
